import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "YouTube Content Summarizer - AI-Powered Video Summaries",
  description:
    "Get instant AI-powered summaries of YouTube videos with streaming responses using Langchain and Next.js",
  keywords:
    "YouTube, summarizer, AI, Langchain, Next.js, video summary, streaming",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
